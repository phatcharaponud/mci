{
  "hosting": {
    "public": "dist",
    "ignore": ["firebase-debug.log", ".*"],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
