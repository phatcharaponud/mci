# Data Schema — รายงาน RCA

ระบบ: RCA Management System โรงพยาบาลมหาวิทยาลัยพะเยา (https://rca-up-hospital.web.app)
แหล่งอ้างอิง: `firebase-rca/public/index.html` — นิยามฟอร์มที่ `const DF` (บรรทัด 253), กลุ่มปัจจัยที่ `const TD` (บรรทัด 226-239), จุดเขียน Firestore ที่ `hSave()` (บรรทัด 2211-2276)

---

## 1. ภาพรวม collection

| Collection | Document ID | ใช้ทำอะไร | จุดเขียนในโค้ด |
|---|---|---|---|
| **`rca`** | `RCA-001`, `RCA-002`, … | **รายงาน RCA (collection หลักของเอกสารนี้)** | `db.collection('rca').doc(newId).set(docData)` / `.set(docData,{merge:true})` |
| `users` | Firebase Auth UID | ผู้ใช้ + สถานะอนุมัติ + PIN | `db.collection('users').doc(uid).set/update` |
| `editRequests` | auto-ID | คำขอร่วมแก้ไข RCA | `db.collection('editRequests').add(...)` |
| `schedules` | `SCH-<base36>` | ปฏิทินกำหนดทำ RCA | `db.collection('schedules').doc(sid).set(...)` |
| `posts` (+ subcollection `replies`) | auto-ID | กระดานแลกเปลี่ยน & RM News | `db.collection('posts').add(...)` |
| `settings` | `ai` (fixed) | Gemini API key | `db.collection('settings').doc('ai').set(...)` |

> ไฟล์แนบไม่ได้เก็บใน Firestore — อัปโหลดขึ้น **Firebase Storage** ที่พาธ `rca-attachments/{rcaId}/{timestamp}_{filename}` แล้วเก็บเฉพาะ URL ลง Firestore

---

## 2. การสร้าง Document ID ของ `rca`

ไม่ใช่ auto-ID ของ Firestore — ระบบอ่านเอกสารทั้ง collection แล้วหาเลขสูงสุดจากรูปแบบ `RCA-(\d+)` บวกหนึ่ง แล้ว pad เป็น 3 หลัก:

```js
const newId = 'RCA-' + String(maxNum+1).padStart(3,'0');   // RCA-001, RCA-002, ...
```

> **ข้อสังเกต:** เป็นการ generate ฝั่ง client โดยไม่มี transaction หากผู้ใช้สองคนกดสร้างพร้อมกัน อาจได้ ID เดียวกันและเขียนทับกันได้
> ฟังก์ชัน `gid()` (`'RCA-'+Date.now().toString(36)`) ยังอยู่ในโค้ดแต่**ไม่ถูกเรียกใช้แล้ว**

---

## 3. Field ที่ระบบสร้างเอง (ไม่มีในฟอร์ม)

| Key | ชนิด | ค่า/ที่มา | เขียนเมื่อไร |
|---|---|---|---|
| `savedAt` | string (ISO 8601) | `new Date().toISOString()` เช่น `"2026-09-01T08:14:22.031Z"` | **ทุกครั้ง**ที่บันทึก — ใช้ `orderBy('savedAt','desc')` เรียงรายการ |
| `status` | string | **`"draft"` เสมอ** (hard-code ไว้ ไม่เคยเปลี่ยนเป็นค่าอื่นทั้งระบบ) | ทุกครั้งที่บันทึก |
| `ownerUid` | string | `user.uid` จาก Firebase Auth | **เฉพาะตอนสร้างใหม่** — ใช้ตัดสินสิทธิ์แก้ไข/ลบใน Firestore Rules |
| `ownerEmail` | string | `user.email` (บังคับ `@up.ac.th`) | เฉพาะตอนสร้างใหม่ |
| `ownerName` | string | `userData.fullName` | เฉพาะตอนสร้างใหม่ — ⚠️ ชนกับ field ในฟอร์ม ดูข้อ 6 |
| `adminViewed` | boolean | `false` ตอนสร้าง → `true` เมื่อ admin กดเปิดดู | สร้าง + ตอน admin เปิด (`hLoad`) |
| `collaborators` | array&lt;string&gt; | UID ผู้ได้รับอนุมัติร่วมแก้ไข (`FieldValue.arrayUnion`) | **ไม่มี field นี้เลย**จนกว่าเจ้าของจะอนุมัติคำขอครั้งแรก |

---

## 4. ตอนที่ 1 — ข้อมูลเบื้องต้น

| Key | Label บนหน้าจอ | ชนิด | Required | หมายเหตุ |
|---|---|---|---|---|
| `department` | หน่วยงาน/ทีมนำ | string | ✅ * | ช่องพิมพ์อิสระ (ไม่ใช่ dropdown — dropdown 37 หน่วยงานใช้เฉพาะตอนลงทะเบียนผู้ใช้) |
| `date` | วันที่จัดทำ (ปี ค.ศ.) | string `YYYY-MM-DD` | – | `<input type="date">` |
| `title` | เรื่อง | string | ✅ * | ชื่อเหตุการณ์ |
| `incidentCode` | รหัสรายงาน | string (ตัวเลขล้วน) | – | กรองอักขระที่ไม่ใช่เลขออกขณะพิมพ์ · ถ้าซ้ำกับรายการอื่น ระบบเตือนแล้วต่อท้ายเป็น `680001(A)`, `(B)`… |
| `incidentType` | ประเภทอุบัติการณ์ | string `^[A-Z]{3}[0-9]{3}$` | – | บังคับรูปแบบขณะพิมพ์ (อังกฤษ 3 ตัวพิมพ์ใหญ่ + เลข 3 ตัว) เช่น `CPE101` |
| `sevL` | ระดับความรุนแรง — NCC MERP (A-I) | string enum | – | **ดูค่าที่เลือกได้ข้อ 4.1** |
| `sevN` | ระดับความรุนแรง — Safety Score (1-5) | string enum | – | **ดูค่าที่เลือกได้ข้อ 4.2** |
| `whatHappened` | 1.1 เกิดอะไรขึ้น | string (หลายบรรทัด) | ✅ * | |
| `where` | 1.2 เกิดที่ | string | – | |
| `when` | 1.3 เวลา | string `HH:MM` | – | `<input type="time">` |
| `eventDate` | วันที่เกิดเหตุ (ปี ค.ศ.) | string `YYYY-MM-DD` | – | |
| `professions` | วิชาชีพที่เกี่ยวข้อง | string (หลายบรรทัด) | – | |
| `relatedDepts` | หน่วยงานที่เกี่ยวข้อง | string (หลายบรรทัด) | – | |
| `mainFixDept` | กลุ่ม/หน่วยงานหลักที่แก้ไข | string | – | |
| `subFixDept` | กลุ่ม/หน่วยงานร่วมที่แก้ไข | string | – | |
| `rcaParticipants` | ผู้เข้าร่วมการทำ RCA | string (หลายบรรทัด, 1 คน/บรรทัด) | – | export PDF แบ่งเป็น 2 คอลัมน์ |
| `rcaDate` | วันที่ทำ RCA (ปี ค.ศ.) | string `YYYY-MM-DD` | – | |
| `rcaTime` | เวลาทำ RCA | string `HH:MM` | – | |
| `attachments` | เอกสารแนบ | **array&lt;object&gt;** | – | ดูข้อ 4.3 |

> ⚠️ **`*` เป็นเครื่องหมายบนหน้าจอเท่านั้น — `hSave()` ไม่มีการตรวจสอบ** เอกสารที่ 3 field นี้ว่างก็บันทึกลง Firestore ได้ (การตรวจ required มีเฉพาะฟอร์มปฏิทิน `schedules`)

### 4.1 `sevL` — NCC MERP

| ค่า | ความหมายย่อ |
|---|---|
| `"A"` | เกิดที่นี่ — มีโอกาสเกิดเหตุการณ์และความคลาดเคลื่อน |
| `"B"` | เกิดที่ไกล — เกิดความผิดพลาดแต่ยังไม่ถึงผู้ป่วย |
| `"C"` | เกิดกับใคร — ถึงผู้ป่วยแต่ไม่เกิดอันตราย |
| `"D"` | ให้ระวัง — ต้องเฝ้าระวังเพิ่ม |
| `"E"` | ต้องรักษา — เกิดอันตรายชั่วคราว ต้องบำบัดรักษา |
| `"F"` | เยียวยานาน — ต้องนอนโรงพยาบาลนานขึ้น |
| `"G"` | ต้องพิการ — เกิดความพิการถาวร |
| `"H"` | ต้องการปั๊ม — ต้องช่วยชีวิต |
| `"I"` | จำใจลา — เสียชีวิต |

### 4.2 `sevN` — Safety Score

| ค่า | ความหมายย่อ |
|---|---|
| `"1"` | ผิดพลาดแต่ไม่มีผลกระทบ |
| `"2"` | มีผลกระทบเล็กน้อย |
| `"3"` | มีผลกระทบปานกลาง |
| `"4"` | ดำเนินงานไม่บรรลุเป้าหมายบางส่วน |
| `"5"` | เสียหายร้ายแรง / ผลกระทบ > 10 ล้านบาท |

### 4.3 `attachments[]` — object ซ้อน

| Key ย่อย | ชนิด | หมายเหตุ |
|---|---|---|
| `name` | string | ชื่อไฟล์เดิม |
| `type` | string | MIME — รับเฉพาะ `image/jpeg`, `image/png`, `application/pdf` |
| `size` | number | ไบต์ — จำกัด ≤ 10 MB (10485760) |
| `url` | string | Download URL จาก Firebase Storage (ว่าง `""` ถ้าอัปโหลดล้มเหลว) |

> ขณะกรอกฟอร์มจะมี key `data` (base64 data URL) อยู่ชั่วคราวใน state แต่ **`hSave()` ตัดทิ้งเสมอ** ไม่เคยถูกเขียนลง Firestore (เพราะเคยทำให้เกิน 1 MB/doc)

---

## 5. ตอนที่ 2-3 — Flow, CMP และปัจจัยสาเหตุ

| Key | Label บนหน้าจอ | ชนิด | หมายเหตุ |
|---|---|---|---|
| `flowSteps` | Care Process / Flow of Steps | **array&lt;string&gt;** | ค่าเริ่มต้น 5 ช่องว่าง `['','','','','']` เพิ่ม/ลบได้ (คงเหลืออย่างน้อย 2) |
| `fsSaved` | (สถานะปุ่ม "บันทึกขั้นตอน") | boolean | `true` เมื่อกดบันทึก · กลับเป็น `false` อัตโนมัติเมื่อแก้ไขต่อ |
| `cmps` | Care Management Problems (CMPs) | **array&lt;object&gt;** | ดูข้อ 5.1 |
| `cmpsSaved` | (สถานะปุ่ม "บันทึก CMP") | boolean | เช่นเดียวกับ `fsSaved` |
| `ta` | ปัจจัยสาเหตุ Tier 1-5 | **array&lt;object&gt; ยาว 12 เสมอ** | ดูข้อ 5.2 |

### 5.1 `cmps[]`

| Key ย่อย | Label | ชนิด | หมายเหตุ |
|---|---|---|---|
| `step` | ขั้นตอน (จาก Flow of Steps) | string | **dropdown** — ตัวเลือกคือค่าใน `flowSteps` ที่ไม่ว่าง (เก็บเป็นข้อความ ไม่ใช่ index) |
| `problem` | ปัญหา (CMP) | string | |
| `unsafeAct` | Unsafe Act / Potential Change | string | |

### 5.2 `ta[]` — 12 กลุ่มปัจจัย (ลำดับคงที่)

array ยาว 12 เสมอ เรียงตรงกับ `TD` ไม่ว่าจะเลือกใช้หรือไม่:

| index | `id` | Label กลุ่ม | Tier |
|---|---|---|---|
| 0 | `t1p` | Tier 1: ผู้ป่วย | 1 |
| 1 | `t1s` | Tier 1: ผู้ปฏิบัติงาน | 1 |
| 2 | `t2t` | Tier 2: ทีมและการสื่อสารในทีม | 2 |
| 3 | `t2w` | Tier 2: ลักษณะงาน | 2 |
| 4 | `t3e` | Tier 3: สิ่งแวดล้อม | 3 |
| 5 | `t3q` | Tier 3: เครื่องมือ / อุปกรณ์ | 3 |
| 6 | `t3s` | Tier 3: การออกแบบระบบงาน | 3 |
| 7 | `t3i` | Tier 3: ข้อมูลและสารสนเทศ | 3 |
| 8 | `t4p` | Tier 4: นโยบาย | 4 |
| 9 | `t4m` | Tier 4: การควบคุม กำกับ สนับสนุน | 4 |
| 10 | `t4c` | Tier 4: บรรยากาศองค์กร | 4 |
| 11 | `t5` | Tier 5: สังคม / นอกองค์กร | 5 |

โครงสร้างแต่ละกลุ่ม:

| Key | Label | ชนิด | หมายเหตุ |
|---|---|---|---|
| `id` | – | string | รหัสกลุ่มตามตารางบน |
| `en` | (ช่องติ๊กหัวข้อ Tier) | boolean | `true` = เกี่ยวข้อง — **เฉพาะกลุ่มที่ `en:true` เท่านั้นที่แสดงใน Export และแผนภาพ** |
| `entries` | รายการที่ 1, 2, … | array&lt;object&gt; | อย่างน้อย 1 เสมอ (แม้ยังไม่กรอก) |

โครงสร้าง `entries[]`:

| Key | Label | ชนิด | ค่าที่เลือกได้ |
|---|---|---|---|
| `ck` | ปัจจัยที่เกี่ยวข้อง | **array&lt;string&gt;** | ติ๊กได้หลายค่า — ข้อความต้องตรงกับ `items` ของกลุ่มนั้นเป๊ะ (ดูข้อ 5.3) |
| `oc` | อื่นๆ ระบุ: (ช่องติ๊ก) | boolean | |
| `ot` | อื่นๆ ระบุ: (ข้อความ) | string | ใช้เมื่อ `oc:true` |
| `desc` | บรรยายสรุปส่วนที่เกี่ยวข้อง | string | |
| `strat` | กลยุทธ์/มาตรการป้องกัน | string | |
| `ind` | เป้าหมาย/ตัวชี้วัด | string | |
| `tl` | เวลาดำเนินการ | string | ข้อความอิสระ (ไม่ใช่ dropdown) |
| `resp` | ผู้รับผิดชอบ | string | |
| `linkedTo` | 🔗 เชื่อมโยงกับ | **object \| null** | ดูข้อ 5.4 |

### 5.3 ค่าที่เลือกได้ของ `ck` แยกตามกลุ่ม

| `id` | ตัวเลือก (`items`) |
|---|---|
| `t1p` | สภาวะทางคลินิก (พยาธิสภาพ/ความซับซ้อน/ความรุนแรง) · ความรู้ความเข้าใจด้านสุขภาพ · ยาที่ใช้อยู่ · ภาษา, สังคม-วัฒนธรรม-ความเชื่อ |
| `t1s` | เหนื่อยล้า · เครียด กดดัน · ละเลย ไม่เอาใจใส่ · ขาดความรู้ / ขาดทักษะ · เอนเอียง/มีอคติในกระบวนการคิด · ขาดสมรรถนะ / ประสบการณ์ · ประเด็นเรื่องสุขภาพ · ทัศนคติ, ความสามารถในการปรับตัว · ไม่กล้าทักท้วงเพื่อความปลอดภัย |
| `t2t` | หน้าที่ไม่ชัดเจน/ขาดความรับผิดชอบ · ไม่ได้แจ้ง-ส่งต่อข้อมูล/ขาดความตระหนัก · ขาดการสื่อสารระหว่างหัวหน้ากับผู้ปฏิบัติงาน · ขาดการสื่อสารระหว่างสมาชิกในทีม · ขาดการสื่อสารระหว่างเจ้าหน้าที่กับผู้ป่วย/ญาติ |
| `t2w` | ความซับซ้อนของกระบวนการทำงาน · ต้องทำงานหลายอย่างด้วยความเร่งรีบ · ถูกรบกวนขณะทำงาน · ต้องใช้ความสามารถเฉพาะบุคคล |
| `t3e` | สิ่งแวดล้อมเชิงกายภาพ เช่น แสง เสียง การถ่ายเทอากาศ · สถานที่คับแคบ/ผังบริเวณไม่เหมาะสม |
| `t3q` | หน้าที่(Function)ไม่เหมาะสม · การออกแบบ(Design)ไม่เหมาะสม · มีไม่พอ / ไม่พร้อมใช้งาน · ขาดการบำรุงรักษา |
| `t3s` | ไม่มีแนวปฏิบัติ / มาตรฐานการปฏิบัติ · แนวปฏิบัติไม่ชัดเจน / ไม่สามารถปฏิบัติได้จริง |
| `t3i` | ไม่มีข้อมูล เข้าถึงข้อมูลยาก · ข้อมูลไม่ชัดเจน อ่านยาก · ข้อมูลไม่ถูกต้อง |
| `t4p` | ไม่มีนโยบาย · นโยบายไม่ชัดเจน · ไม่ทำตามนโยบาย/แนวทาง/ระเบียบปฏิบัติ |
| `t4m` | ขาดการควบคุม กำกับ สนับสนุนด้านคลินิก · ขาดการควบคุม กำกับ สนับสนุนด้านการจัดการ |
| `t4c` | วัฒนธรรมความปลอดภัยไม่เหมาะสม · การจัดการ/การควบคุมเชิงวิศวกรรมไม่เหมาะสม |
| `t5` | นโยบาย กฎหมาย ระเบียบข้อบังคับระดับชาติ · อิทธิพลของสังคม · เทคโนโลยีใหม่ / การออกแบบอุปกรณ์ |

### 5.4 `linkedTo` — 3 รูปแบบ

ใช้วาดลูกศรในแผนภาพ 2-Axis Swiss Cheese (ข้อ 4 ของแถบสรุปผล และไวท์บอร์ด interactive)

```jsonc
null                                              // ไม่เชื่อมโยง
{ "type": "cmp",  "index": 0 }                    // ชี้ไป cmps[0]
{ "type": "tier", "tierId": "t3s", "entryIndex": 0 }  // ชี้ไป ta[<กลุ่ม t3s>].entries[0]
```

> เชื่อมโยงได้เฉพาะ **CMP หรือ Tier ที่ต่ำกว่า** (ห้ามชี้ไป Tier เดียวกันหรือสูงกว่า) · เมื่อลบ CMP/entry ระบบจะ **ปรับ index ที่เหลือให้อัตโนมัติ** และเคลียร์ลิงก์ที่ชี้ไปของที่ถูกลบเป็น `null`

---

## 6. แถบสรุปผลและติดตาม

| Key | Label บนหน้าจอ | ชนิด | ค่าที่เลือกได้ |
|---|---|---|---|
| `summaryDate` | ส่งข้อมูลกลับวันที่ | string `YYYY-MM-DD` | – |
| `daysToSubmit` | จำนวนวัน | string enum | `""` (เลือก) · `"3"` 3 วัน · `"7"` 7 วัน · `"14"` 14 วัน · `"30"` 1 เดือน · `"30+"` มากกว่า 1 เดือน |
| `followUpMonth` | เดือนที่ติดตามผล | string `YYYY-MM` | `<input type="month">` — ใช้กรอง "แสดงรายการติดตามในเดือนที่เลือก" |
| `followUpStatus` | สถานะการติดตาม | string enum | `""` ยังไม่ได้ติดตาม · `"in_progress"` กำลังดำเนินการ · `"completed"` ติดตามเสร็จสิ้น |
| `ownerName` | เจ้าของอุบัติการณ์ | string | ⚠️ ดูคำเตือนด้านล่าง |
| `headName` | หัวหน้าหน่วยงานที่เกี่ยวข้อง | string | แสดงเป็นช่องลายเซ็นใน Export |
| `secretaryName` | เลขาความเสี่ยง | string | แสดงเป็นช่องลายเซ็นใน Export |
| `chairName` | ประธานความเสี่ยง | string | แสดงเป็นช่องลายเซ็นใน Export |
| `hrms` | เตือน: ลงระบบ HRMS (ช่องติ๊ก) | boolean | อยู่แถบบนสุดของฟอร์ม ติ๊กได้จากทุกแท็บ |

> ⚠️ **`ownerName` ใช้ซ้อนกัน 2 ความหมาย** — เป็นทั้งช่องกรอก "เจ้าของอุบัติการณ์" (แถบสรุปผล) และเป็น field ที่ระบบเขียนทับด้วย `userData.fullName` ตอนสร้างเอกสารครั้งแรก
> ผลคือ: **บันทึกครั้งแรก** ค่าที่ผู้ใช้พิมพ์จะถูกแทนที่ด้วยชื่อผู้ล็อกอิน · **บันทึกครั้งถัดไป** ค่าที่พิมพ์จะถูกเก็บตามจริง
> คอลัมน์ "ผู้จัดทำ" ในหน้ารายการ RCA และการค้นหาก็อ่าน field นี้ ทำให้ชื่อผู้จัดทำเปลี่ยนได้ถ้าผู้ใช้แก้ช่องนี้ — หากต้องการชื่อผู้จัดทำที่เชื่อถือได้ ควรใช้ `ownerUid` / `ownerEmail` แทน

---

## 7. สรุปจำนวน field

| หมวด | จำนวน key ระดับบนสุด |
|---|---|
| ตอนที่ 1 | 19 |
| ตอนที่ 2-3 | 5 |
| สรุปผลและติดตาม | 9 |
| *(รวม field ที่มาจากฟอร์ม = `DF`)* | *33* |
| ระบบเพิ่มเองตอนบันทึก | 6 |
| **รวมทั้งเอกสาร** | **39** |

> `ownerName` นับอยู่ในหมวด "สรุปผลและติดตาม" (เป็น field ของฟอร์ม) แม้ระบบจะเขียนทับตอนสร้างเอกสารครั้งแรก จึงไม่นับซ้ำในหมวดระบบ — field ที่ระบบเพิ่มใหม่จริงมี 6 ตัวคือ `ownerUid`, `ownerEmail`, `adminViewed`, `collaborators`, `savedAt`, `status`

โครงสร้างซ้อน: `attachments[]` (4 key ย่อย), `cmps[]` (3), `ta[]` (3 → `entries[]` 9 key ย่อย), `flowSteps[]` (array ของ string)

---

## 8. ตัวอย่างการอ่านข้อมูล

```js
// รายการทั้งหมด (ทุกคนอ่านได้ แก้ไขได้เฉพาะเจ้าของ/ผู้ร่วมแก้ไข/admin)
db.collection('rca').orderBy('savedAt','desc').onSnapshot(...)

// สิทธิ์แก้ไข (ตรงกับเงื่อนไขใน firestore.rules)
const canEdit = isAdmin
  || r.ownerUid === user.uid
  || (r.collaborators||[]).includes(user.uid);
```

ดูตัวอย่างเอกสารเต็มที่กรอกครบทุก field ได้ที่ **`rca-sample.json`**
